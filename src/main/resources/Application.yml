spring:
  application:
    name: ToDoApi
  datasource:
    url: jdbc:h2:mem:tarefas
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      path: /h2-console